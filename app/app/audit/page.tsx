import { FileText } from "lucide-react";
import { PlaceholderPage } from "@/components/placeholder-page";

export default function AuditPage() {
  return (
    <PlaceholderPage
      title="Audit Log"
      description="Compliance and forensics tracking for all system actions"
      icon={FileText}
    />
  );
}
