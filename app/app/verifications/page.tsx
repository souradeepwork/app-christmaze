import { ShieldCheck } from "lucide-react";
import { PlaceholderPage } from "@/components/placeholder-page";

export default function VerificationsPage() {
  return (
    <PlaceholderPage
      title="Verifications"
      description="Tenant review queue for compliance and onboarding"
      icon={ShieldCheck}
    />
  );
}
